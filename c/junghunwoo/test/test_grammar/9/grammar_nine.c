
#include "grammar_nine.h"
#include "../utility/random.h"

int *alloc_array(int *count)
{
    *count = generate_random(4, 8);

    int *tmp_array = (int *)malloc(sizeof(int) * (*count));

    for (i = 0;)
    {
        ddd
    }

    return tmp_array;
}
#include "vector.h"
#include "../../utility/random.h"

#include <stdlib.h>

vector *init_vector_object(void)
{
    vector *tmp_vector = (vector*)malloc(sizeof(vector));
    return tmp_vector;
}


void vector_info_allocation(vector* vector_array)
{
    
}
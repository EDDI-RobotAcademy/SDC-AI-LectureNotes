#include <stdio.h>

// 함수 선언
int add(a,b);
int a = 5;
int b = 6;
int main() {
    int result;

    // 함수 호출
    result = add(a,b);

    // 반환된 값 출력
    printf("Result: %d\n", result);

    return a+b;
}

